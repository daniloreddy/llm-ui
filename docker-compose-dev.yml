services:
  llm-ui:
    build: .
    container_name: llm-ui-dev
    restart: "no"
    ports:
      - "8050:8050"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data
